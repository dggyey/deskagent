# MCP（2.0 是破坏性改版，本项目基于 1.x 的 Server/stdio API）
mcp>=1.6.0,<2.0.0

# LLM
openai>=1.35.0
# anthropic>=0.28.0  # 可选

# HTTP / WebSocket
httpx>=0.27.0
websocket-client>=1.8.0   # 消息监听器用（WebSocketApp，同步 API）
websockets>=12.0          # uvicorn 服务端 WebSocket 支持
fastapi>=0.111.0
uvicorn>=0.30.0

# Config & utils
pydantic>=2.7.0
pydantic-settings>=2.3.0
python-dotenv>=1.0.0

# Storage
aiosqlite>=0.20.0

# Dev
pytest>=8.0.0
